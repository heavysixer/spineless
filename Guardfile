# A sample Guardfile
# More info at https://github.com/guard/guard#readme
# guard "shell" do
#   watch('spineless.js') do 
#     `rocco -l js spineless.js && mv ./spineless.html ./docs/index.html`
#     `curl -s -d compilation_level=SIMPLE_OPTIMIZATIONS -d output_format=text -d output_info=compiled_code --data-urlencode "js_code@spineless.js" http://closure-compiler.appspot.com/compile > spineless.min.js`
#   end
# end

guard 'coffeescript', :input => 'coffee', :output => '.'
